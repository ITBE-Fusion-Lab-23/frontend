import React from "react";
declare type Dispatch = React.Dispatch<React.SetStateAction<{}>>;
declare type State = Record<string, number>;
declare type RatingProviderProps = {
    children: React.ReactNode;
};
declare const RatingProvider: ({ children }: RatingProviderProps) => JSX.Element;
declare const useForceUpdate: () => {
    registeredButtons: State;
    forceUpdate: Dispatch;
};
export { RatingProvider, useForceUpdate };
